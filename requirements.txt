# RAG-LAW — Streamlit Community Cloud compatible dependencies
# Install with: pip install -r requirements.txt
#
# ⚠️  IMPORTANT: The extra-index-url below forces CPU-only PyTorch (~200 MB)
#     instead of the default CUDA build (~2.5 GB) which breaks Streamlit Cloud.

--extra-index-url https://download.pytorch.org/whl/cpu

# ── PyTorch (CPU-only — must come before sentence-transformers) ───────────────
torch==2.2.2+cpu
torchvision==0.17.2+cpu

# ── Streamlit ────────────────────────────────────────────────────────────────
streamlit>=1.43.0,<2.0.0

# ── LangChain core stack ─────────────────────────────────────────────────────
langchain==0.3.17
langchain-community==0.3.16
langchain-core==0.3.33
langchain-text-splitters==0.3.5

# ── LLM: Groq ────────────────────────────────────────────────────────────────
langchain-groq==0.2.4
groq==0.16.0

# ── Embeddings: HuggingFace (CPU-only, no API key required) ──────────────────
langchain-huggingface==0.1.2
sentence-transformers==3.4.1
transformers>=4.39.3,<5.0.0
tokenizers>=0.15.0

# ── Vector store ─────────────────────────────────────────────────────────────
faiss-cpu>=1.7.4

# ── PDF loading ───────────────────────────────────────────────────────────────
pdfplumber==0.11.4
pypdf==5.3.0

# ── HTTP / async ─────────────────────────────────────────────────────────────
httpx>=0.27.0
anyio>=4.0.0
